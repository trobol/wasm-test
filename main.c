#include <wasm.h>

	int length;
	f32 square(float a)
	{
		return a * a;
	}
	/*
	const float *setup(int length) {
		array = float[length];
		return array;
	}

	void test() {
		for(int i = 0; i < length; i++) {
			
		}
	}
	*/
